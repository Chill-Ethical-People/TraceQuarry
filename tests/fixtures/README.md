# Synthetic Parser Fixtures

Everything under this directory is intentionally fabricated for automated
parser tests. The files are text-only, use reserved documentation indicators
or obvious placeholders, and contain no data from a collected system.

Never add real UAC output, customer evidence, credentials, executable payloads,
or generated investigation results here. Reduce a parser regression to the
smallest synthetic record needed to demonstrate the behavior.
